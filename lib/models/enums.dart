enum ServiceStatus {
  booking,
  waiting,
  inProgress,
  waitingForParts,
  finalCheck,
  done,
}

enum ServiceType { routine, major, partReplacement }

enum ProductCategory {
  oil,
  fluids,
  engineParts,
  brakes,
  tiresWheels,
  interiorAccessories,
  exteriorAccessories,
  electronics,
}

